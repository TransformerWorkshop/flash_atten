# PT Systematic Test Report - 2026-04-12 (Asia/Shanghai)

## Commands
1. `make -C sim/iverilog sim top=tb_pt`
2. `make -C sim/iverilog sim top=tb_quant`
3. `make -C sim/iverilog sim top=tb_pt_qcfg_odd`
4. `make -C sim/iverilog sim top=tb_gemm`
5. `make -C sim/iverilog sim top=tb_gemm_col`

## Results
- `tb_pt`: `TB RESULT (PT): PASS`
- `tb_quant`: `TB RESULT (QUANT): PASS`
- `tb_pt_qcfg_odd`: `TB RESULT (PT_QCFG_ODD): PASS`
- `tb_gemm`: `TB RESULT (ROW): PASS`
- `tb_gemm_col`: `TB RESULT (COL): PASS`

## Bug List
- No blocking bugs found.

## Non-blocking Warnings / Risks
- `iverilog` warnings are present across runs:
  - missing `timescale` in multiple RTL modules;
  - `@*` sensitivity warnings for array reads (for example `pt_md.v` LUT arrays and `pt_mem.v` lane arrays).
- These warnings did not block simulation and all targeted PT regressions passed.

## Conclusion
- PT core + dependency regression suite passed end-to-end on branch `codex`.
- Current PT changes are functionally stable under the exercised scenarios.
